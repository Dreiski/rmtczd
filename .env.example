# Postgres connection string (Neon).
# Leave unset locally to use the bundled PGlite database in .pglite/ instead.
DATABASE_URL=

# Optional: override where the local PGlite fallback stores its data.
# PGLITE_DIR=.pglite
